[project]
name = "telco-streaming-quality"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "apache-beam==2.74.0",
    "confluent-kafka>=2.15.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]

[tool.pytest.ini_options]
pythonpath = ["."]